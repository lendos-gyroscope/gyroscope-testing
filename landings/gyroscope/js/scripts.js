"use strict";

$(function () {
    var requestUri = document.location.pathname;
    var requestParams = document.location.search;

    console.log("requestUri: " + requestUri );
    console.log("requestParams: " + requestParams);
});